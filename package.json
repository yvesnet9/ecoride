{
  "name": "ecoride-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n web,api -c blue,magenta \"npm --prefix frontend run dev\" \"npm --prefix backend-express run dev\"",
    "web": "npm --prefix frontend run dev",
    "api": "npm --prefix backend-express run dev",
    "install:all": "npm install --prefix frontend && npm install --prefix backend-express"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}


{
  "name": "ecoride-monorepo",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "concurrently -n web,api -c blue,magenta \"npm --prefix apps/web run dev\" \"npm --prefix apps/api run start:dev\"",
    "start:web": "npm --prefix apps/web run dev",
    "start:api": "npm --prefix apps/api run start:dev",
    "build": "npm --prefix apps/web run build && npm --prefix apps/api run build",
    "lint": "eslint .",
    "test": "npm --prefix apps/web run test && npm --prefix apps/api run test"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "eslint": "^9.0.0",
    "typescript": "^5.9.3",
    "vite": "^7.1.9"
  }
}
